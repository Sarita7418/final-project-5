import React from "react";
import LoginBox from "./LoginBox";
import "./Login.css"; // CSS general para el contenedor

const Login = () => {
  return (
    <div className="login-container">
      <LoginBox />
    </div>
  );
};

export default Login;
